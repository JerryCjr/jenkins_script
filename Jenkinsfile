Jenkinsfile (Declarative Pipeline)
pipeline {
  agent any
  stages {
    stage ('Jerry Build') {
      steps {
        sh 'echo "baby first step"'
        sh 'echo "小小改动一下 看是否能出发jenkins构建"'
      }
    }
  }
}
